// import logger_N from "./index"; //Es^ new way (not supported in browsers yet)

// const logger_N = require('./index') // old way but supported everywhere

let Symbol = "|0|"
let slideSymbol = "\\.\\"
let starCount = 20

logger_N(starCount, Symbol, slideSymbol)